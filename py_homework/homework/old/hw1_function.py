"""
作業 1：函式（def）基本功  ——  這是最重要的一關，不要跳過
難度 ★☆☆☆☆   預估 30 分鐘   共 5 小題

=================================================================
【先讀這段：函式到底在幹嘛】
=================================================================

函式 = 一台機器。你丟東西進去，它吐東西出來。

    def add(a, b):        # def 機器名字(輸入1, 輸入2):
        return a + b      # return = 吐出結果

    答案 = add(3, 5)      # 丟 3 和 5 進去，答案接住吐出來的 8
    print(答案)           # 8

-----------------------------------------------------------------
【最容易搞混的：return 和 print 差在哪】
-----------------------------------------------------------------

    def add_print(a, b):
        print(a + b)      # 印在螢幕上「給人看」，程式本身拿不到這個值

    def add_return(a, b):
        return a + b      # 把值「交還給程式」，可以繼續拿去算

    x = add_print(3, 5)   # 螢幕出現 8，但 x 是 None（空的！）
    y = add_return(3, 5)  # 螢幕什麼都沒有，但 y = 8

    print(y * 2)          # 16  <- 因為 y 真的有值
    print(x * 2)          # 錯誤！None 不能乘 2

    一句話記住：函式負責「算」(return)，主程式負責「印」(print)。

-----------------------------------------------------------------
【還有一個坑：return 之後的程式碼不會執行】
-----------------------------------------------------------------

    def f(n):
        return n
        print("我永遠印不出來")   # return 一執行，函式立刻結束

=================================================================
【5 小題】把每個 pass 換成你的程式碼
=================================================================
"""


# --- 第 1 題 ---------------------------------------------------
# 寫一個函式 double(n)，回傳 n 的兩倍。
# 提示：只需要一行 return
def double(n):
    pass


# --- 第 2 題 ---------------------------------------------------
# 寫一個函式 is_even(n)，n 是偶數回傳 True，奇數回傳 False。
# 提示：n % 2 == 0 這整串本身就是 True 或 False，可以直接 return 它
def is_even(n):
    pass


# --- 第 3 題 ---------------------------------------------------
# 寫一個函式 bigger(a, b)，回傳比較大的那個。
# 提示：用 if / else，兩個分支各自 return
# 規定：不准用內建的 max()，這題就是要練 if + return
def bigger(a, b):
    pass


# --- 第 4 題 ---------------------------------------------------
# 寫一個函式 total(number_list)，回傳 list 裡所有數字的總和。
# 提示：先 s = 0，再用 for 把每個數加進 s，最後 return s
# 規定：不准用內建的 sum()
def total(number_list):
    pass


# --- 第 5 題 ---------------------------------------------------
# 寫一個函式 count_even(number_list)，回傳 list 裡「偶數的個數」。
# 提示：這題可以呼叫你自己在第 2 題寫的 is_even()！函式是可以互相呼叫的。
def count_even(number_list):
    pass


# =================================================================
# 以下是測試，不要改。全部寫對的話執行後會看到 5 個 OK
# =================================================================
print("第1題", "OK" if double(5) == 10 and double(0) == 0 else f"錯，double(5)給了 {double(5)}，應該是 10")
print("第2題", "OK" if is_even(4) is True and is_even(7) is False else f"錯，is_even(4)給了 {is_even(4)}，應該是 True")
print("第3題", "OK" if bigger(3, 9) == 9 and bigger(10, 2) == 10 else f"錯，bigger(3,9)給了 {bigger(3, 9)}，應該是 9")
print("第4題", "OK" if total([1, 2, 3, 4]) == 10 and total([]) == 0 else f"錯，total([1,2,3,4])給了 {total([1, 2, 3, 4])}，應該是 10")
print("第5題", "OK" if count_even([1, 2, 3, 4, 6]) == 3 else f"錯，count_even([1,2,3,4,6])給了 {count_even([1, 2, 3, 4, 6])}，應該是 3")
