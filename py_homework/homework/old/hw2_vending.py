"""
作業 2：把你的販賣機改成函式版
難度 ★★☆☆☆   預估 40 分鐘
前置：先寫完 hw1_function.py

=================================================================
【你原本的 vendingmachine.py 有什麼問題】
=================================================================

    menu = {"漢堡":"100", ...}       <- 價格是「字串」，每次用都要 int() 轉
    cart.append(int(menu.get(food))) <- cart 只存價格，結帳印不出買了什麼
    food = input(...).lower()        <- 中文用 .lower() 沒有任何作用

這題把這 3 點修掉，順便把「一整段從上跑到下」拆成函式。

-----------------------------------------------------------------
【新東西 1：in 可以直接檢查 key 在不在 dict 裡】
-----------------------------------------------------------------

    menu = {"漢堡": 100, "薯條": 30}

    你原本寫的：  if menu.get(food) is None:   # 可以，但繞
    比較直覺的：  if food not in menu:         # 讀起來就是「不在菜單裡」

-----------------------------------------------------------------
【新東西 2：cart 存「品名」而不是「價格」】
-----------------------------------------------------------------

    cart = ["漢堡", "可樂", "漢堡"]

    好處：品名可以查回價格 menu["漢堡"] -> 100，
          但價格 100 查不回品名。存資訊多的那個永遠比較好。

    要總價就把品名一個個查回來相加：
        for name in cart:
            total = total + menu[name]

=================================================================
【規格】寫這 3 個函式 + 一個 main()
=================================================================

  print_menu(menu)
      印出每一項「品名:價格」。不用 return。

  checkout(cart, menu)
      算出總金額並 return。
      重點：這個函式裡面【一個 print 都不准有】，它只負責算。

  show_cart(cart, menu)
      印出購物車每一項的「品名 價格」，最後印總金額
      （總金額直接呼叫 checkout 拿，不要重算一遍）

  main()
      1. 先 print_menu
      2. while 迴圈問使用者要買什麼
         - 輸入 q     -> break
         - 輸入 list  -> 呼叫 show_cart
         - 在菜單裡   -> cart.append(品名)，印「已加入 漢堡」
         - 不在菜單裡 -> 印「商品不存在」（不可以當掉）
      3. 迴圈結束後呼叫 show_cart 印最後明細

【自我檢查】
  [ ] menu 的價格是 100 不是 "100"
  [ ] checkout 裡面沒有 print，只有 return
  [ ] 買兩個漢堡，總金額 200
  [ ] 亂打「牛排」不會讓程式崩潰
  [ ] 全檔縮排都是 4 個空格（你原本的檔案 else 底下只縮 1 格）
"""

menu = {"漢堡": 100, "薯條": 30, "雞腿": 60, "雞塊": 40, "可樂": 30, "紅茶": 15}


def print_menu(menu):
    # TODO: for item, price in menu.items(): ...
    pass


def checkout(cart, menu):
    # TODO: 算總金額，return 它。這裡不要 print
    pass


def show_cart(cart, menu):
    # TODO
    pass


def main():
    cart = []
    print_menu(menu)
    # TODO: while True: ...
    pass


main()
