"""
作業 4：修好你的猜拳遊戲（不用 class，純函式）
難度 ★★★☆☆   預估 50 分鐘
前置：hw1, hw2, hw3

=================================================================
【你原本的 paperscissor.py 有 3 個 bug，先看懂再動手】
=================================================================

bug 1  電腦整場出同一拳
    answer = random.choice(computer)   <- 這行在 while 迴圈「外面」
    while run == True:
        ...
    只抽一次，之後每一輪都用同一個 answer。
    修法：把抽拳搬進迴圈裡面。

bug 2  布 vs 石頭 印出 "剪刀"
    elif player == "布":
        if answer == "石頭":
            print("剪刀")      <- 複製貼上忘了改，應該是 win

bug 3  有一段程式永遠執行不到
    elif player == "布":
        if answer == "石頭":
            print("剪刀")
        if answer == "石頭":   <- 又是石頭！第二個 if 不可能成立
            print("win")

    bug 2 跟 bug 3 都是同一個原因：**複製貼上一堆 if**。
    只要規則有 9 種組合，用 if 疊就一定會有漏。下面教你怎麼根治。

-----------------------------------------------------------------
【新東西：用 dict 描述規則，取代一堆 if】
-----------------------------------------------------------------

    BEATS = {"剪刀": "布", "石頭": "剪刀", "布": "石頭"}
             ^^^^^^^^^^^^  意思是「剪刀 打敗 布」

    有了這張表，判斷勝負只要 3 行：

        if player == computer:
            結果是平手
        elif BEATS[player] == computer:     # 玩家的拳打敗電腦的拳
            結果是玩家贏
        else:
            結果是玩家輸

    9 種組合、0 次複製貼上、不可能漏掉。
    這就是「把規則放進資料」的威力 —— 比寫在 if 裡面安全得多。

-----------------------------------------------------------------
【小提醒：list(dict) 可以拿到所有 key】
-----------------------------------------------------------------

    list(BEATS)              ->  ["剪刀", "石頭", "布"]
    random.choice(list(BEATS))  ->  隨機抽一拳，不用另外開一個 list

    順便：判斷玩家輸入合不合法也用這張表
        if player not in BEATS:   -> 不是剪刀/石頭/布

=================================================================
【規格】
=================================================================

  BEATS
      上面那張字典，自己填好。

  judge(player, computer)
      回傳字串 "draw" / "win" / "lose"（win = 玩家贏）。
      這個函式裡【不准有 print】，只負責判斷。
      規定：if 最多 3 個（用 BEATS，不要疊 9 種情況）。

  play_round()
      1. 用 random.choice 抽電腦的拳（每一輪都要重抽！）
      2. 印出雙方出拳
      3. 呼叫 judge 拿結果，印出中文的「你贏了 / 你輸了 / 平手」
      4. return 結果字串（main 要拿來計分）

  main()
      wins = loses = draws = 0
      while 迴圈：
        - 問玩家出拳，輸入 q 就 break
        - 不在 BEATS 裡 -> 印「請輸入 剪刀/石頭/布」，continue（這局不算）
        - 呼叫 play_round，依回傳值把對應的計數 +1
      結束後印統計：「X勝 Y敗 Z平，勝率 NN.N%」

      注意：一局都沒玩就按 q，算勝率會 ZeroDivisionError，要先判斷。
      勝率算法：wins / (wins + loses + draws) * 100
      印成一位小數：f"{勝率:.1f}%"

【自我檢查】
  [ ] 連玩 5 局，電腦出拳不是每次都一樣（bug 1 修掉）
  [ ] 出「布」對上電腦「石頭」-> 你贏了（bug 2 修掉）
  [ ] judge 裡面的 if 不超過 3 個（bug 3 根治）
  [ ] judge 裡面沒有 print
  [ ] 一局都沒玩直接按 q，不會紅字當掉
"""

import random

BEATS = {}  # TODO: 填上「誰打敗誰」


def judge(player, computer):
    # TODO: return "draw" / "win" / "lose"
    pass


def play_round():
    # TODO
    pass


def main():
    # TODO
    pass


main()
